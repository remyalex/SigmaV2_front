import { PrevenirMultiplesClicksDirective } from './prevenir-multiples-clicks.directive';

describe('PrevenirMultiplesClicksDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevenirMultiplesClicksDirective();
    expect(directive).toBeTruthy();
  });
});
